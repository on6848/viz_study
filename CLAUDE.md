# viz_study — 運用ガイド（Claude向け）

このリポジトリは「毎日1本論文を読み、自分の言葉でまとめ、動く可視化デモで直感的に理解できるサイトを作る」
プロジェクトです。ユーザーの操作は基本的に **(1) 論文を渡す** と **(2) 手法・デモへの改善提案を出す** の
2つだけにしたい、という方針です。つまり、論文が渡されたら以降の作業（要約作成・デモ実装・サイトへの
組み込み・commit/push）はほぼ全てこちらで完結させること。過剰に確認を挟まない。

## 論文が渡されたときにやること

1. **内容を理解する。** PDF/URL/テキストなど渡された形式に応じて読み込む。
2. **オリジナルの要約を書く。** `papers/2026-09-15-attention-is-all-you-need/index.html` を
   フォーマットの見本にする。以下のセクション構成を基本とする（日本語、常体・平易な文章）:
   - 一言でいうと
   - 解きたかった問題
   - 提案手法の核心
   - 触って理解する：可視化デモ
   - 結果・インパクト
   - 限界・批判点
   - 学び・所感
   - **原論文の文章・図表をそのまま転載しない。** 自分の理解で言い換える。
3. **可視化デモを1つ以上作る。** 論文の核心アイデアが「触って直感的にわかる」ことを最優先する。
   - 依存ライブラリなし（vanilla JS/SVG）か、cdnjs等のCDN読み込みのみ。ビルド不要でGitHub Pagesに
     そのまま置けること。
   - チャートを作る前に **dataviz skill** を読み、フォームの選び方・配色・ホバー/ツールチップ・
     アクセシビリティ（表での代替表示など）のルールに従う。配色は `assets/css/style.css` の
     CSS変数（`--series-1`〜`--series-8`、`--seq-100`〜`--seq-700` 等）を使う。
   - デモに使うデータが論文の実データではなく説明用の合成データの場合、その旨をページ内に明記する
     （`.callout.note-source` を使う）。事実に基づかない印象を与えないこと。
   - 既存デモ（Attention Is All You Need）のように、ヘッダー説明・凡例・ホバーツールチップ・
     キーボード操作・表での代替表示を備えること。
4. **`papers.json` にエントリを追加する。** `slug` はページのフォルダ名と一致させる
   （`YYYY-MM-DD-短い英語スラッグ`）。日付は実際にその日論文を読んだ日付を使う。
5. **`papers/_template/` をコピーして新しいページを作る**（テンプレートの使い方は
   `papers/_template/README.md` 参照）。
6. **commit & push する。** ユーザーから明示の指示がなくても、この一連の作業が終わったら
   commitしてpushしてよい（このプロジェクトの性質上、都度の確認は不要という前提）。
   コミットメッセージ例: `Add paper: <論文タイトル>`。
7. 完了したら、要約の要点とデモで何が体感できるかを一言でユーザーに報告する。

## 改善提案が来たときにやること

- 「このデモはこう見せた方がいい」「この説明はもっと簡潔に」といった指摘は、該当ページの
  `index.html` / `demo.js` を直接修正し、commit & pushする。
- 修正の意図が曖昧なときのみ確認する。基本は指摘の意図を汲んで直接手を動かす。

## 守るべき方針

- **著作権・引用**: 図表・本文を転載しない。要約・デモは独自作成。原論文へのリンク（arXiv等）を
  必ず記載する。
- **誠実さ**: デモが合成データを使う場合は明示する。論文の主張を誇張・歪曲しない。
- **一貫性**: 新しいページも `papers/_template/` の構成・`assets/css/style.css` のトークンに
  揃える。フレームワーク追加やビルドツール導入はしない（ビルド不要の静的サイトを維持する）。
- **公開**: GitHub Pages（`main`ブランチ、root）を想定。Pages未設定ならユーザーに設定を促す
  （Settings → Pages → Source: `main` / `/`）。
- **キャッシュ対策**: 全ページの `assets/css/style.css` / `assets/js/site.js` の読み込みには
  `?v=YYYYMMDDx` のクエリを付けている（例: `?v=20260916a`）。これら共有ファイルを変更したら、
  参照している全HTML（`index.html`、`papers/*/index.html`、`papers/_template/index.html`）の
  バージョン文字列を新しい値に一括で上げること（さもないとブラウザ/GitHub PagesのCDNキャッシュにより
  古い見た目のまま反映されないことがある）。`papers.json` は `assets/js/site.js` 側で
  タイムスタンプ付きfetchをしているため個別のバージョニングは不要。
